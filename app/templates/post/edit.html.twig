{% include 'default/_form.html.twig' with {
    page_title: 'title.edit_post'|trans({'%id%': post.id|default('')}),
    form: form,
    submit_label: 'action.edit'|trans,
    back_to_list_path: 'post_index',
} %}